# Maplefile for examples/minimal
#

# Name of the local container
container="create"

# Name of the target directory to mount inside the container
target="/home/mount"

# Container backend
backend="docker"

# Default run command
run="echo Run!"

# Default execute commands
execute=["echo Hello World!",
         "echo Second line."]
