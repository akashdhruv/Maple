name: create

on:
  pull_request:
    branches: 
      - main
    paths-ignore:
      - '**.md'
      - 'LICENSE'
      - 'CITATION'
      - 'docs/'

jobs:
  example:
    name: "example"
    uses: ./.github/workflows/maple-env.yml@development
    steps:
    - uses: eWaterCycle/setup-singularity@v7
    - name: Run examples/create
      run: |
          cd examples/create
          maple image build
          maple container execute
          maple image clean
          maple image remove
          python3 MapleAPI.py
